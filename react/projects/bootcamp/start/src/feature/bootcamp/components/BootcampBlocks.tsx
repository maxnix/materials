/* eslint-disable react/no-array-index-key */
import { Block } from "@/service/api/bootcamp/types"

export const BootcampBlocks = ({ blocks }: { blocks: Block[] }) => (
  <div className="flex flex-col gap-4">Blocks</div>
)
